<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kirannaidu.studentbenfits">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Home" />
        <activity android:name=".Educational" />
        <activity android:name=".Information" />
        <activity android:name=".SubjectEntry" />
        <activity android:name=".Claasses_list" />
        <activity android:name=".ClassList1" />
        <activity android:name=".SubjectsLists" />
        <activity android:name=".AddSubFile" />
        <activity android:name=".ViewFile" />
        <activity android:name=".User1" />
        <activity android:name=".User2" />
        <activity android:name=".User3" />
        <activity android:name=".UploadFile"></activity>
    </application>

</manifest>